import { ImgBrokenDirDirective } from './img-broken-dir.directive';

describe('ImgBrokenDirDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgBrokenDirDirective();
    expect(directive).toBeTruthy();
  });
});
